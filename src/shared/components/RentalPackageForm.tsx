
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from './ui/card';

export const RentalPackageForm: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Rental Package Form</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Rental package form coming soon...</p>
      </CardContent>
    </Card>
  );
};
