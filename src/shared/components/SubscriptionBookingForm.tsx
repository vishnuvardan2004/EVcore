
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from './ui/card';

export const SubscriptionBookingForm: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Subscription Booking Form</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Subscription booking form coming soon...</p>
      </CardContent>
    </Card>
  );
};
